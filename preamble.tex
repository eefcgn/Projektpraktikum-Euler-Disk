\usepackage{amsmath} % for the math formulas
\usepackage{graphicx} % for inserting and scaling the images so ts doesnt look retarded
\usepackage{float} % so that the images can be exactly where they are on latex file with [H]
\usepackage[backend=biber,style=numeric,sorting=none]{biblatex}
\addbibresource{bibliography/references.bib}